name: Build Bonsai

on:
  push:
    branches: ["main"]
    paths:
      - "2026-01-21-tulip-downstream-distributions/distributions/bonsai/**"
  pull_request:
    branches: ["main"]
    paths:
      - "2026-01-21-tulip-downstream-distributions/distributions/bonsai/**"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v7
        with:
          go-version: "1.24"

      - name: Build distribution
        run: make build
        working-directory: 2026-01-21-tulip-downstream-distributions/distributions/bonsai

      - name: Validate configuration
        run: make validate
        working-directory: 2026-01-21-tulip-downstream-distributions/distributions/bonsai
